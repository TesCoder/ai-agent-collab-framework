# Events dictionary (template)

Define the canonical analytics events for the project here.

## Table

| Event name | When it fires | Required params | Optional params | Notes |
|-----------|----------------|-----------------|-----------------|-------|
| `example_event` | Example trigger | `cta_location` | `experiment_id` | - |

## Rules

- Event names are stable once shipped.
- All Work Packets that mention analytics must reference this file.
